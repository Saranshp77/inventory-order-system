# Serves the built React app and supports client-side routing:
# any unknown path falls back to index.html so React Router can handle it.
server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }
}
